{
  "scripts": {
    "generate-test-data": "node backend/scripts/generateTestData.js",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "deploy": "echo 'Triggering Vercel deployment'"
  },
  "dependencies": {
    "@google/genai": "^1.0.1",
    "concurrently": "^8.2.2",
    "crypto": "^1.0.1",
    "ethers": "^6.13.5"
  }
}
